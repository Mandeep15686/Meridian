# ── Core framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.4
pydantic-settings==2.2.1

# ── Async / task queue ────────────────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.0.4
httpx==0.27.0
anyio==4.3.0

# ── LangChain / LangGraph ─────────────────────────────────────────────────────
langchain==0.2.1
langgraph==0.2.0
langchain-anthropic==0.1.15
langchain-openai==0.1.8
langsmith>=0.1.112,<0.2.0

# ── LlamaIndex ────────────────────────────────────────────────────────────────
llama-index==0.10.40
llama-index-embeddings-openai==0.1.10
llama-index-vector-stores-postgres==0.1.12

# ── Database (Windows-safe versions) ─────────────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
# Use pre-built psycopg2-binary wheel (no pg_config needed on Python 3.12)
psycopg2-binary==2.9.9
pgvector==0.2.5
alembic==1.13.1

# ── Anthropic + OpenAI ────────────────────────────────────────────────────────
anthropic==0.30.1
openai==1.30.1

# ── HuggingFace ───────────────────────────────────────────────────────────────
huggingface-hub==0.23.0
transformers==4.41.0
sentence-transformers==3.0.0
tokenizers==0.19.1

# ── Audio (webrtcvad skipped — Windows wheel often unavailable) ───────────────
librosa==0.10.2
pydub==0.25.1
mutagen==1.47.0
soundfile==0.12.1
# webrtcvad is optional — the code falls back to fixed-size chunking without it

# ── Vision / image ────────────────────────────────────────────────────────────
Pillow==10.3.0
pdf2image==1.17.0
python-magic-bin==0.4.14

# ── Document processing ───────────────────────────────────────────────────────
pypdf==4.2.0
python-docx==1.1.2
openpyxl==3.1.3
pandas==2.2.2
numpy==1.26.4

# ── ML / tabular ──────────────────────────────────────────────────────────────
scikit-learn==1.4.2
torch==2.3.0

# ── Report generation (weasyprint skipped — GTK deps complex on Windows) ──────
Jinja2==3.1.4
markdown==3.6
# Install weasyprint separately if you need PDF reports (see below)

# ── Eval / observability ──────────────────────────────────────────────────────
ragas==0.1.10
datasets==2.19.1
mlflow==2.13.0
wandb==0.17.0

# ── Utilities ─────────────────────────────────────────────────────────────────
ulid-py==1.1.0
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
tenacity==8.3.0
structlog==24.2.0
rich==13.7.1
typer==0.12.3

# ── UI ────────────────────────────────────────────────────────────────────────
streamlit==1.35.0